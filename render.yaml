# Render Blueprint for the static MEXC web app
services:
  - type: static
    name: sellbitweb-mexc

    rootDir: .

    buildCommand: npm ci && npm run build
    staticPublishPath: dist

    autoDeployTrigger: commit
